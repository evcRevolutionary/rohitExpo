import styled from 'styled-components/native';
import { View } from 'react-native';

export const MapConatiner = styled(View)({
	flex: 1,
	position: 'relative',
});

export const MapConatinerInfo = styled(View)({
	flex: 1,
	width: 100,
	height: '100%',
});

export const MapMarkerView = styled(View)({
	height: '100%',
});
